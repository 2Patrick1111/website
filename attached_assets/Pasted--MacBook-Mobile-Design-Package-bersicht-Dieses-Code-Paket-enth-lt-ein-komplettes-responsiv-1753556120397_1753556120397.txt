# MacBook & Mobile Design Package

## Übersicht

Dieses Code-Paket enthält ein komplettes, responsives Design-System mit realistischen MacBook- und iPhone-Mockups. Es kann einfach in jede Website oder Anwendung integriert werden.

## Features

### 🖥️ Desktop (MacBook Design)
- **Realistisches MacBook-Design** mit 3D-Perspektive
- **Browser-Fenster** mit macOS-Style Traffic Lights
- **Funktionale Tastatur** mit allen Tasten (F1-F12, QWERTY, etc.)
- **Interaktives Trackpad** mit Hover-Effekten
- **Authentische Materialien** (Aluminum-Look, schwarze Tasten)

### 📱 Mobile (iPhone Design)
- **iPhone-ähnlicher Rahmen** mit abgerundeten Ecken
- **Notch** am oberen Rand
- **Home-Button** am unteren Rand
- **Responsive Content-Bereich**
- **Touch-optimierte Buttons**

### 🎯 Responsive Design
- **Automatischer Wechsel** zwischen Desktop- und Mobile-Ansicht
- **Optimierte Layouts** für verschiedene Bildschirmgrößen
- **Fluid Transitions** zwischen den Ansichten

## Installation & Verwendung

### 1. Einfache Integration
```html
<!-- Kopiere den gesamten Inhalt von macbook-mobile-design.html -->
<!-- Oder binde es als externe Datei ein: -->
<iframe src="macbook-mobile-design.html" width="100%" height="800px"></iframe>
```

### 2. Als Komponente einbetten
```html
<!-- Nur die relevanten CSS-Klassen und HTML-Strukturen kopieren -->
<div class="device-container">
    <!-- Desktop Browser -->
    <div class="browser-container">
        <div class="browser-window">
            <!-- Dein Content hier -->
        </div>
    </div>
    
    <!-- Mobile Phone -->
    <div class="mobile-phone">
        <div class="mobile-content">
            <!-- Dein Mobile Content hier -->
        </div>
    </div>
    
    <!-- MacBook Keyboard -->
    <div class="macbook-keyboard">
        <!-- Tastatur-Struktur -->
    </div>
</div>
```

## Anpassungen

### Content ändern
```javascript
// Browser-Content anpassen
document.querySelector('.browser-content').innerHTML = `
    <h1>Dein Titel</h1>
    <p>Dein Inhalt</p>
`;

// Mobile-Content anpassen
document.querySelector('.mobile-content').innerHTML = `
    <h2>Mobile Titel</h2>
    <p>Mobile Inhalt</p>
`;
```

### Styling anpassen
```css
/* Browser-Größe ändern */
.browser-window {
    width: 1200px; /* Standard: 900px */
    height: 800px; /* Standard: auto */
}

/* Mobile-Größe ändern */
.mobile-phone {
    width: 360px; /* Standard: 320px */
    height: 640px; /* Standard: 568px */
}

/* Farben anpassen */
.browser-content,
.mobile-content {
    background: linear-gradient(135deg, #your-color-1 0%, #your-color-2 100%);
}
```

## CSS-Klassen Übersicht

### Haupt-Container
- `.device-container` - Wrapper für das gesamte Design
- `.browser-container` - Container für Desktop-Browser
- `.mobile-phone` - Mobile Phone Frame

### Browser-Komponenten
- `.browser-window` - Hauptfenster
- `.browser-header` - Header mit Traffic Lights
- `.traffic-lights` - macOS Fenster-Buttons
- `.address-bar` - URL-Leiste
- `.browser-content` - Content-Bereich

### MacBook-Komponenten
- `.macbook-keyboard` - Hauptcontainer der Tastatur
- `.keyboard-base` - Tastatur-Basis (Aluminum)
- `.keyboard-rows` - Container für Tastenreihen
- `.keyboard-row` - Einzelne Tastenreihe
- `.key` - Einzelne Taste
- `.trackpad` - Trackpad

### Mobile-Komponenten
- `.mobile-content` - Content-Bereich im Phone
- `.mobile-buttons` - Button-Container
- `.mobile-btn` - Mobile Buttons

## Responsive Breakpoints

```css
/* Desktop */
@media (min-width: 769px) {
    /* MacBook und Browser sichtbar */
    /* Mobile Phone versteckt */
}

/* Mobile */
@media (max-width: 768px) {
    /* Mobile Phone sichtbar */
    /* MacBook und Browser versteckt */
}
```

## Interaktivität

### JavaScript-Events
```javascript
// Tastatur-Klicks
document.querySelectorAll('.key').forEach(key => {
    key.addEventListener('click', function() {
        console.log('Taste:', this.textContent);
    });
});

// Trackpad-Klicks
document.querySelector('.trackpad').addEventListener('click', function() {
    console.log('Trackpad geklickt');
});
```

### Demo-Funktionen
- `changeContent(page)` - Ändert Desktop-Content
- `changeMobileContent(page)` - Ändert Mobile-Content

## Browser-Kompatibilität

✅ **Unterstützt:**
- Chrome 60+
- Firefox 55+
- Safari 11+
- Edge 79+

⚠️ **Eingeschränkt:**
- Internet Explorer (keine backdrop-filter Unterstützung)

## Performance-Tipps

1. **CSS-Transforms optimieren:**
```css
.macbook-keyboard {
    will-change: transform;
    transform: perspective(800px) rotateX(25deg);
}
```

2. **Smooth Scrolling:**
```css
.browser-content,
.mobile-content {
    scroll-behavior: smooth;
}
```

3. **Hardware-Beschleunigung:**
```css
.key {
    transform: translateZ(0);
}
```

## Lizenz

Dieses Design-Package kann frei verwendet und angepasst werden.

## Support

Bei Fragen zur Implementation oder Anpassungen, dokumentiere die gewünschten Änderungen und die Zielplattform.